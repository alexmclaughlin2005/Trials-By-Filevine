[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npx prisma generate --schema=./packages/database/prisma/schema.prisma",
  "npm run build --workspace=@juries/notification-service"
]

[start]
cmd = "npm run start --workspace=@juries/notification-service"
