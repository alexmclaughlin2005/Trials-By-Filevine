{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node --version && yarn install --frozen-lockfile && npx prisma generate --schema=./packages/database/prisma/schema.prisma && npm run build --workspace=@juries/database && npm run build --workspace=@juries/notification-service"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=@juries/notification-service",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
