{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate --schema=./packages/database/prisma/schema.prisma && npm run build --workspace=@juries/collaboration-service"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=@juries/collaboration-service",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
