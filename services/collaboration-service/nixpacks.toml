[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd ../.. && npm install"]

[phases.build]
cmds = ["cd ../.. && npm run build --workspace=@juries/collaboration-service"]

[start]
cmd = "cd ../.. && npm run start --workspace=@juries/collaboration-service"
